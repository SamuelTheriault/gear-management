Django==5.2.16
djangorestframework==3.17.1
django-environ==0.14.0
django-cors-headers==4.9.0
PyMySQL==1.2.0
gunicorn==26.0.0
whitenoise==6.12.0
# Assainissement du HTML des notes (2026-08-05) — successeur de bleach,
# recommandé par ce dernier depuis son abandon. Voir inventory/rich_text.py :
# le nettoyage se fait à l'écriture, pas à l'affichage.
nh3==0.3.6
django-allauth==65.18.0
dj-rest-auth==7.2.0
# Dépendances runtime du provider Google de django-allauth — non déclarées par
# allauth/dj-rest-auth eux-mêmes, mais requises dès le démarrage de Django
# (SocialAccountConfig.ready() importe le provider Google au chargement de
# l'app, même sans requête). Sans elles, `python manage.py check` (et donc
# tout déploiement Railway) plante immédiatement avec un ModuleNotFoundError.
requests==2.34.2
PyJWT==2.13.0
cryptography==49.0.0
# Génération des codes QR imprimés sur les sorties de rapport (2026-08-08).
# Python pur, zéro dépendance, sortie SVG vectorielle — voir
# inventory/report_shares.qr_svg pour les raisons de ne pas passer par un
# service tiers comme api.qrserver.com.
segno==1.6.6
# Rendu PDF des sorties de rapport (2026-08-08). Exige trois paquets système
# dans l'image — libpango-1.0-0, libpangoft2-1.0-0, libharfbuzz-subset0 —
# ajoutés au Dockerfile. L'import est différé dans inventory/report_pdf.py
# pour qu'un paquet manquant n'empêche pas Django de démarrer.
weasyprint==69.0
